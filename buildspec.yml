version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - mvn package
      - mv target/fluxnews-api-0.0.1-SNAPSHOT.war fluxnews-api-0.0.1-SNAPSHOT.war
      - echo Build completed on `date`
#artifacts:
  #files:
    #- fluxnews-api-0.0.1-SNAPSHOT.war
    #- .ebextensions/**/*
  #base-directory: 'target/fluxnews-api-0.0.1-SNAPSHOT'